{
    assignment_1 :{
        task : "
            The array `names` contains various names.

            Please complete in order that the array names_without_voyal contains the names wich does not conain the letter `a`
            and then log the new array.
            "
        ,
        skeleton : "
            var names = ['rie' , 'asako', 'stefano', 'kyoko', 'lia'];
            var names_no_a = [];
            // write your code here
            console.log(names_no_a);
            "
        ,
        solution :  "
            var names = ['rie' , 'asako', 'stefano', 'kyoko', 'lia'];
            var names_no_a = [];
            for( var i = 0; i < names.length; i++ ){
              if( names[i].indexOf("a") === -1)  {
                names_no_a.push(names[i]);
              }
            }
            console.log(names_no_a);
            "
        ,
        antiCheat : "
            var variables = ""
            for (var name in this)
                variables += name + "\n";
            ",
    }
}